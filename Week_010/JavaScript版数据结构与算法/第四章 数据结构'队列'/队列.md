#### **<font color=red>队列是什么?</font>**

+ 一个先进先出的数据结构
+ JavaScript中没有队列,但可以用Array实现队列的所有功能.

#### **<font color=red>什么场景用队列</font>**

+ 需要先进先出的场景
+ 比如: 食堂排队打饭、JS异步中的任务队列、计算最近请求次数.

<font color=red>计算最近请求次数</font>

+ 有新请求就入队,3000ms前发出的请求出队.(获取对头q[0])
+ 队列的长度就是请求次数.